// Same thing here...this seems to be the only index file for components
// We should either keep this pattern and make the index.ts into index.tsx that exports the component code
// Or keep importing `@/components/Component/Component` everywhere
export { default } from './Spinner';
